<div class="container my-5 w-50 ">
  <h3>Add a new site</h3>
  <%= simple_form_for [@tour, @site] do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit class: 'btn btn-flat' %>
  <% end %>
</div>
