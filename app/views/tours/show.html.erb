
<h2>tours#show</h2>

<h3><%= @tour.name %></h3>
<p><%= @tour.description %></p>

<ul>
  <% @tour.sites.each do |site| %>
  <li> <%= link_to site.name, site_path(site) %></li>
  <% end %>
</ul>

<%= render "sites/form" %>
