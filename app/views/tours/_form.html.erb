  <%= simple_form_for @tour do |f| %>
    <%= f.input :name  %>
    <%= f.input :description %>
    <%= f.input :privacy, as: :select, collection: [['private',true], ['public',false]] %>
    <%= f.input :photo, as: :file %>
    <%= f.submit class: 'button button-primary' %>
  <% end %>
