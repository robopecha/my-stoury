<div class="container my-5 w-50 ">
  <h3>Open a chatroom</h3>
  <%= simple_form_for [@tour, @chatroom] do |f| %>
    <div class="form-inputs">
    <%= f.input :name %>
    </div>
    <%= f.submit class: 'btn btn-flat' %>
  <% end %>
</div>
